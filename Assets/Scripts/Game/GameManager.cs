using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    //private NPCSpawner _npcSpawner;
    
    private void Awake()
    {
        //_npcSpawner = (NPCSpawner) FindObjectOfType(typeof(NPCSpawner));
        
        //SetRandomSeed.Init();
        //NPCDataGenerator.Init();
        //_npcSpawner.Init();
    }
}
